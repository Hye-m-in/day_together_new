<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.day_together">

    <!-- Android 13+(API 33)에서 갤러리 이미지 접근 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- 33+에서 READ_EXTERNAL_STORAGE는 폐지되므로 32까지만 허용 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 네트워크 통신 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Android 13+(API 33) 이상에서 푸시/알림 표시 허용 필요 -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="하루함께"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Day_together"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="35"
        android:name=".DayTogetherApp"> <!-- targetSdk=35에 맞춰 tools:targetApi도 35로 상향 / Application 클래스 등록 -->

        <!-- 앱의 시작점을 MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Day_together">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 네이버 OAuth 브릿지 액티비티 (네이버 로그인 콜백 수신용)
             - 스킴은 '네이버 클라이언트 ID' 그대로 사용
             - build.gradle의 manifestPlaceholders 에서 naverClientId 값을 주입함 -->
        <activity
            android:name="com.navercorp.nid.oauth.NidOAuthBridgeActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="${naverClientId}"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
